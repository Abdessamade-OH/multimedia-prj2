import { RemoveFirstLetterPipe } from './remove-first-letter.pipe';

describe('RemoveFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
